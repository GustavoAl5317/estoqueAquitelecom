# Arquivos que rodam no Debian precisam sair com fim de linha LF, mesmo tendo
# sido editados no Windows: um `.sh` com CRLF falha no shell com um erro que
# não diz o que houve ("cannot execute: required file not found").
*.sh text eol=lf
*.service text eol=lf
*.conf text eol=lf
